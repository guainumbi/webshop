<% @orders.each do |order| %>
    <% order.items.each do |item| %>
    <%= item.title %>
    <%= number_to_currency(
      item.price,
      unit: item.currency,
      separator: ",",
      delimiter: ".",
      format: "%n %u") %>
    <% end %>
    <br>
<% end %>
